import numpy as np

# Create an array of 20 random values
random_array = np.random.random(20)

# Multiply each value in the array by π
multiplied_by_pi = random_array * np.pi

# Display the result
print(multiplied_by_pi)
